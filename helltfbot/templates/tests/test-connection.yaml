apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helltfbot.fullname" . }}-test-connection"
  labels:
    {{- include "helltfbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helltfbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
