apiVersion: v1
kind: Service
metadata:
  name: {{ include "helltfbot.fullname" . }}
  labels:
    {{- include "helltfbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helltfbot.selectorLabels" . | nindent 4 }}
